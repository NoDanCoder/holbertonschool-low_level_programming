#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char dummie[] = "\"Programming is like building a multilingual puzzle";

	puts(dummie);

	return (0);
}


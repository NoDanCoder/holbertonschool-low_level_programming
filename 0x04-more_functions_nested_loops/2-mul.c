#include "holberton.h"

/**
 * mul - multiply parameters
 * @a: first digit to multiply
 * @b: second digit to multiply
 *
 *
 * Return: result of "a * b"
 * On error: -1 inapropiate entry
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
